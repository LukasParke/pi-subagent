import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

/** Extension wiring is integrated after runner/registry/policy/UI modules. */
export default function registerSubagent(_pi: ExtensionAPI): void {}
